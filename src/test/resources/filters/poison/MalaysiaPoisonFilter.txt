log.debug("StartWithMalaysiaPoisonFilter");

        String[] split = name.split(" ");
        if ("MALAYSIA".equalsIgnoreCase(split[0])) {
            log.debug("Found Invalid! " + scrubbedName);
            return true;
        }
        return false;
    }
