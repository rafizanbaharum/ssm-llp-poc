import java.util.*;
import com.ssm.llp.core.model.*;

log.debug("SymbolPoisonFilter");

// check contains
for (SsmName symbol : nameDao.find(6)) {
    if(name.contains(symbol.getName()))
        return true;
}
return false;
