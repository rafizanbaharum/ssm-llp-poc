log.debug("MalaysianStatePoisonFilter");

      String[] states = nameDao.getStates();
        for (String state : states) {
            if (name.toUpperCase().startsWith(state)) {
                log.debug("Found Invalid! " + state);
                log.debug("Name = " + name);
                return true;
            }
        }
        return false;
