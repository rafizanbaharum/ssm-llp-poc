log.debug("StatePoisonFilter");

// check as a whole
return nameDao.hasName(name.toUpperCase(), 2);

// check word for word
String[] words = name.toUpperCase().split(" ");
for(int i=0; i < words.length; i++){
      log.debug("words: " + words[i]);
      if(nameDao.hasName(words[i], 2)) return true;
}
return false;