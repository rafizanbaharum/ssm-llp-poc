log.debug("NonStandaloneCountryPoisonFilter");

        String[] countries = nameDao.getCountries();
        for (String country : countries) {
            if (name.toUpperCase().startsWith(country)) {
                log.debug("Found Invalid! " + country);
                return true;
            }
        }
        return false;