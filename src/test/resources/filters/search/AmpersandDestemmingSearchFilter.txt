log.debug("AmpersandStemmingSearchFilter");
if(name.contains("&")) {
  name = name.replace("&","dan");
  log.debug("name after stemming: " + name);
  return companyDao.hasName(name);
}
else return false;
