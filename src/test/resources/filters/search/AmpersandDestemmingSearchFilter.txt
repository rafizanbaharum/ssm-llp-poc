log.debug("AmpersandStemmingSearchFilter");
if(name.contains("&")) {
  temp = name.replace("&","");
  log.debug("name after stemming: " + temp);
  return companyDao.hasName(temp);
}
else return false;
